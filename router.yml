---
- name: Setup routing services
  hosts: router
  gather_facts: no

  environment: "{{env_vars}}"

  roles:
    - setup
    - role: docker-services
      vars:
        services:
          - Pihole
