---
- name: Setup self hosted services
  hosts: all
  gather_facts: no

  environment:
    DOMAIN_NAME: "{{domain_name}}"

  roles:
    - setup
    - traefik
    - portainer
    - heimdall
    - jellyfin
    - nextcloud
    - privatebin
