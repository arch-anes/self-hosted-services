---
- name: Setup self hosted services
  hosts: all
  gather_facts: no

  vars_files:
    - common.yml

  tasks:
    - name: Copy docker-compose files
      copy:
        src: .
        dest: "{{ remote_project_location }}"

- import_playbook: traefik/playbook.yml
- import_playbook: privatebin/playbook.yml
