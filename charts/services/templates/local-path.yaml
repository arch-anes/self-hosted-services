{{- if .Values.primaryTenant }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path
  annotations:
    storageclass.kubernetes.io/is-default-class: "false"
    defaultVolumeType: local

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path-ephemeral
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
    defaultVolumeType: local
provisioner: rancher.io/local-path
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Delete
allowVolumeExpansion: true
parameters:
  nodePath: "{{ .Values.storageLocation }}"
  pathPattern: "{{`cluster-local-storage/{{ .PVName }}_{{ .PVC.Namespace }}_{{ .PVC.Name }}`}}"

---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path-persistent
  annotations:
    defaultVolumeType: local
provisioner: rancher.io/local-path
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Retain
allowVolumeExpansion: true
parameters:
  nodePath: "{{ .Values.storageLocation }}"
  pathPattern: "{{`{{ .PVC.Name }}`}}"
{{- end }}
