- name: "Deploy {{stack_display_name}} stack"
  docker_stack:
    state: present
    name: "{{stack_name}}"
    compose:
      - "{{remote_stacks_location}}/{{stack_name}}/docker-compose.yml"
  ignore_errors: yes
